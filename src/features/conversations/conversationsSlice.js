import { createSlice } from "@reduxjs/toolkit";

const initialState={

}


 const conversatiosSlice=createSlice({
    name:'conversations',
    initialState,
    reducers:{

    }


})

export const{}=conversatiosSlice.actions

export default conversatiosSlice.reducer